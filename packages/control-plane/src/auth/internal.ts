/**
 * Internal API authentication for service-to-service calls.
 *
 * Re-exports from @CodInspect/shared for consistency across packages.
 */

export { generateInternalToken, verifyInternalToken } from "@CodInspect/shared";
